def process_file(address):
	# loads scores into memory
	f = (address, 'r')
	format = str.split(f, "\n")

def save_score(address,name,score):
	# saves a score to the end of the file
	print("CHJHHCS")

def print_scoreboard(scores):
	print sorted(scores)

chk = {'sam':40,'someone else': 25, 'another person': 12}
print_scoreboard(chk)
